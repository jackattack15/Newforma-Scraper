Note: Selenium Webdriver with Chromium is needed to run this program.

To-Do:
-For some reason the program is crashing at RFI 811, 837, 842 Just to name a few spots.
  -Need to figure out why and correct it

-Implement a GUI
  -Select directory to save Excel output file to, as well as file name with a default option
  -pop-ups to log in
  -status bar while program is running
  -progress and output in an optional 'Details' scrollable dropdown while running

-Implment a method for program to still write Excel File even if it crashes part way through

-Implement a method to let you know if it skipped a field

-Implement a method to skip over problem RFIs and let you know which ones it skipped

-Make it a standalone executable?